# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real keys.

# Existing (used by translator.py / tts.py / speech_to_text.py)
SARVAM_API_KEY=
# NEW — required for the Emergency > Nearby Facilities / Ambulance features.
# Create this key in Google Cloud Console with the "Places API (New)" enabled.
# Restrict it by server IP — it is only ever called from this backend.

